Operations commands are now working correctly:
uv run python -m voicebridge operations list
Info: No active operations

All operations commands (list, cancel, status) are fully functional with the progress service initialized.
